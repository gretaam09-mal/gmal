# Prompts

Versioned prompt templates used by `backend/services/extraction` and
`backend/services/composition`. Every prompt that ever touches a number must
still route that number through `backend/engine/` — see
`docs/CONVENTIONS.md` rule 1. Prompts here only produce narrative text and
structured extraction, never arithmetic.

Empty in Phase 1 — no prompts have been written yet.
