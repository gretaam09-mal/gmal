# Runbooks

Operational how-tos. `local-development.md` covers running everything
locally; more runbooks (deploys, incident response) land as the
corresponding infrastructure exists — see `infra/aws/main.tf`.
